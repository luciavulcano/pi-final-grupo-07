# Banco de dados

O projeto usa H2 com `spring.jpa.hibernate.ddl-auto=update`, portanto as tabelas são criadas automaticamente na primeira execução.

Não é necessário instalar Oracle, MySQL ou PostgreSQL para apresentar o projeto na faculdade.
